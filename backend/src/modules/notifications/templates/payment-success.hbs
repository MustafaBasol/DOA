<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Ödeme Başarılı</title>
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
      line-height: 1.6;
      color: #333;
      background-color: #f5f5f5;
      margin: 0;
      padding: 0;
    }
    .container {
      max-width: 600px;
      margin: 40px auto;
      background-color: #ffffff;
      border-radius: 8px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
      overflow: hidden;
    }
    .header {
      background: linear-gradient(135deg, #10b981 0%, #059669 100%);
      color: white;
      padding: 30px;
      text-align: center;
    }
    .header h1 {
      margin: 0;
      font-size: 24px;
      font-weight: 600;
    }
    .success-icon {
      font-size: 64px;
      margin-bottom: 10px;
    }
    .content {
      padding: 40px 30px;
    }
    .amount-box {
      background: linear-gradient(135deg, #10b981 0%, #059669 100%);
      color: white;
      padding: 30px;
      text-align: center;
      border-radius: 8px;
      margin: 20px 0;
    }
    .amount-box .amount {
      font-size: 48px;
      font-weight: 700;
      margin: 10px 0;
    }
    .amount-box .label {
      font-size: 14px;
      opacity: 0.9;
    }
    .info-table {
      background-color: #f9fafb;
      padding: 20px;
      border-radius: 6px;
      margin: 20px 0;
    }
    .info-row {
      display: flex;
      justify-content: space-between;
      padding: 12px 0;
      border-bottom: 1px solid #e5e7eb;
    }
    .info-row:last-child {
      border-bottom: none;
    }
    .info-label {
      font-weight: 600;
      color: #6b7280;
    }
    .info-value {
      color: #111827;
    }
    .footer {
      background-color: #f8f9fa;
      padding: 20px 30px;
      text-align: center;
      font-size: 12px;
      color: #666;
    }
    .footer a {
      color: #667eea;
      text-decoration: none;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <div class="success-icon">✅</div>
      <h1>Ödeme Başarılı!</h1>
    </div>
    
    <div class="content">
      <p>Merhaba {{userName}},</p>
      <p>Ödemeniz başarıyla işleme alındı. Hizmetiniz aktif durumda.</p>
      
      <div class="amount-box">
        <div class="label">Ödenen Tutar</div>
        <div class="amount">{{amount}} {{currency}}</div>
      </div>
      
      <div class="info-table">
        <div class="info-row">
          <span class="info-label">Plan:</span>
          <span class="info-value">{{planName}}</span>
        </div>
        <div class="info-row">
          <span class="info-label">Tutar:</span>
          <span class="info-value">{{amount}} {{currency}}</span>
        </div>
        <div class="info-row">
          <span class="info-label">İşlem No:</span>
          <span class="info-value">{{transactionId}}</span>
        </div>
      </div>
      
      <p style="color: #666; font-size: 14px;">
        Faturanızı panel üzerinden indirebilirsiniz. Herhangi bir sorunuz olursa destek ekibimizle iletişime geçebilirsiniz.
      </p>
      
      <p style="color: #10b981; font-weight: 600; text-align: center; margin-top: 30px;">
        Teşekkür ederiz! 🎉
      </p>
    </div>
    
    <div class="footer">
      <p>© {{year}} DOA WhatsApp Manager. Tüm hakları saklıdır.</p>
      <p>
        <a href="mailto:support@autoviseo.com">Destek</a> •
        <a href="mailto:billing@autoviseo.com">Fatura</a>
      </p>
    </div>
  </div>
</body>
</html>
